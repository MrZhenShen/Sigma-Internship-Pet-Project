version: '3.8'
services:
  db:
    image: mysql
    ports:
      - 3306:3306
    restart: always
    environment:
      MYSQL_DATABASE: internship
      MYSQL_ROOT_PASSWORD: root

volumes:
  db:
    driver: local